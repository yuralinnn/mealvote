# 部署到 Render — 照著點就好

全程免費，**不需要信用卡**。大約 15 分鐘，其中一半是等它跑。

---

## 你需要先有的東西

- 一個 GitHub 帳號（沒有的話到 [github.com/join](https://github.com/join) 辦，兩分鐘）
- 電腦上有 git（Mac 內建。在終端機打 `git --version` 有東西出來就是有）

---

## 第 1 步：把程式碼放上 GitHub

**1-1. 建一個空的 repo**

到 [github.com/new](https://github.com/new)：

- Repository name 填 `mealvote`
- 選 **Private**（不想被人看到的話）或 Public 都可以
- **下面三個勾選框全部不要勾**（Add a README、.gitignore、license 都不要）
- 按 **Create repository**

**1-2. 把資料夾推上去**

解壓縮我給你的 zip，然後在終端機：

```bash
cd 你解壓縮的位置/mealvote
git remote add origin https://github.com/你的帳號/mealvote.git
git branch -M main
git push -u origin main
```

> 程式碼裡已經有兩個 commit 了，你不用自己 `git init` 或 `git commit`。
>
> 如果 push 時要你輸入密碼，那個密碼**不是** GitHub 登入密碼，是 Personal Access Token。
> 到 [github.com/settings/tokens](https://github.com/settings/tokens) → Generate new token (classic) →
> 勾 `repo` → 產生出來的那串就是密碼。

推完之後重新整理 GitHub 頁面，應該看得到檔案。

---

## 第 2 步：註冊 Render

到 [render.com](https://render.com)，按 **Get Started** → 用 **GitHub 帳號登入**。

用 GitHub 登入的好處是它直接就能讀到你的 repo，不用再授權一次。

---

## 第 3 步：一次建好網站＋資料庫

專案裡有一個 `render.yaml`，Render 看得懂，會照著它把東西建好，你不用一格一格填。

1. Dashboard 右上角 **New +** → 選 **Blueprint**
2. 找到 `mealvote` 這個 repo → 按 **Connect**
   - 找不到的話按 **Configure account**，把 repo 的存取權限給 Render
3. 它會列出要建立的東西：
   - `mealvote` — Web Service（網站本身）
   - `mealvote-db` — PostgreSQL（存房間資料）
4. Blueprint Name 隨便填，按 **Apply**

**為什麼要資料庫？** Render 免費方案的檔案系統是暫時的，服務閒置 15 分鐘會休眠，
醒來時檔案會回到剛部署的樣子——也就是大家填到一半的資料會全部不見。
這個 app 的使用情境（早上開房間、傍晚才投票）中間一定會閒置，所以一定要有資料庫。

`render.yaml` 裡已經把資料庫的連線字串接到網站了，**你不用複製貼上任何東西**。

---

## 第 4 步：等它跑完

第一次大約 2–4 分鐘。

進到 `mealvote` 這個服務，看右上角的狀態：

- **Building** → 正在安裝套件
- **Deploying** → 正在啟動
- **Live** ✅ → 好了

網址在服務名稱下面，長得像 `https://mealvote-xxxx.onrender.com`。

---

## 第 5 步：確認資料庫真的接上了

**這一步很重要，不要跳過。**

瀏覽器打開 `https://你的網址/healthz`，應該看到：

```json
{"ok":true,"storage":"pg","rooms":0}
```

- `"storage":"pg"` ✅ 資料庫接上了，重啟資料還在
- `"storage":"file"` ❌ 沒接上。去服務的 **Logs** 分頁看，會有一行 `⚠️ 連不上 DATABASE_URL`，
  後面接著錯誤原因。最常見是資料庫還在建立中，等兩分鐘按 **Manual Deploy → Deploy latest commit** 再試一次。

然後開網址本身，應該看到「🍽️ 朋友約飯神器」的首頁，
最下面會寫「收錄雙北 323 家餐廳 · 台北捷運 139 站」。

---

## 第 6 步：讓它不要一直睡著（選做，但建議做）

免費方案閒置 15 分鐘會休眠，下一個人點連結要等大約一分鐘才醒過來。
**資料不會掉**（在資料庫裡），只是等待很尷尬，朋友會以為你給的連結壞了。

免費解法：

1. 到 [cron-job.org](https://cron-job.org) 註冊（免費，不用信用卡）
2. **Create cronjob**
3. URL 填 `https://你的網址/healthz`
4. 執行間隔選 **every 10 minutes**
5. 存檔

這樣它就會一直醒著。

> 免費方案每個月有 750 小時額度，一直醒著大約用掉 744 小時，**剛好在額度內，但只夠一個服務**。
> 所以不要在同一個帳號下再開第二個一直醒著的免費服務。

---

## 第 7 步：分享

網址直接丟群組就好。

房間連結本身就是入場券，不需要登入、不用註冊、不收 Email。

建議自己先跑一次完整流程（開房間 → 用手機無痕視窗當第二個人 → 產生推薦 → 投票 → 看結算），
確認都正常再丟給朋友。

---

## 三十天後會發生什麼事

Render 的**免費 PostgreSQL 建立後 30 天到期**。到期後資料庫連不上，
服務不會掛掉（它會自動退回檔案模式繼續跑），但重啟就會掉資料。

到期時這樣處理，大約三分鐘：

1. Dashboard → **New +** → **PostgreSQL** → 免費方案，建一個新的
2. 建好之後複製它的 **Internal Database URL**
3. 到 `mealvote` 服務 → **Environment** → 把 `DATABASE_URL` 的值換成剛剛複製的
4. 按 **Save, rebuild, and deploy**
5. 舊的資料庫可以刪掉

**不用搬資料。** 這個 app 的房間本來就是一次性的，程式碼裡也設定 30 天前的房間會自動清掉——
到期那天剛好等於全部重來，沒有東西要保留。

---

## 之後要改東西怎麼辦

改完程式碼之後：

```bash
git add -A
git commit -m "改了什麼"
git push
```

Render 偵測到 push 會自動重新部署，不用回去按任何東西。

---

## 出問題的時候看哪裡

| 症狀 | 看哪裡 |
|---|---|
| 部署失敗 | 服務的 **Logs** 分頁，往上捲找紅色的錯誤 |
| 網站打得開但功能怪怪的 | 一樣看 **Logs**，同時開瀏覽器的開發者工具看 Console |
| `/healthz` 顯示 `"storage":"file"` | Logs 裡搜尋 `⚠️`，會直接寫連不上的原因 |
| 第一次點很慢 | 正常，是休眠醒來。做了第 6 步就不會了 |
| 房間不見了 | 先確認 `/healthz` 是不是 `"storage":"file"`。是的話資料庫沒接上 |
