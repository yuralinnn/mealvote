# 部署到 Vercel — 照著點就好

全程免費，**不需要信用卡**。大約 20 分鐘，其中一半是等它跑。

用兩個服務：

- **Vercel** — 放網站本身。免費方案叫 Hobby，僅限非商業用途（朋友約飯完全符合）。
- **Neon** — 放資料（PostgreSQL）。免費方案**是永久的，不是試用**，不會 30 天到期。

---

## 你需要先有的東西

- 一個 GitHub 帳號（沒有的話到 [github.com/join](https://github.com/join) 辦，兩分鐘）
- 電腦上有 git（Mac 內建。終端機打 `git --version` 有東西出來就是有）

---

## 第 1 步：把程式碼放上 GitHub

**1-1. 建一個空的 repo**

到 [github.com/new](https://github.com/new)：

- Repository name 填 `mealvote`
- Private 或 Public 都可以
- **下面三個勾選框全部不要勾**（Add a README、.gitignore、license 都不要）
- 按 **Create repository**

> ⚠️ 要建在**你自己的帳號底下**，不要建在某個 organization 裡面。
> Vercel 的免費方案不支援連接 organization 擁有的 repo。

**1-2. 把資料夾推上去**

解壓縮我給你的 zip，然後在終端機：

```bash
cd 你解壓縮的位置/mealvote
git remote add origin https://github.com/你的帳號/mealvote.git
git branch -M main
git push -u origin main
```

> 程式碼裡已經有 commit 了，你不用自己 `git init` 或 `git commit`。
>
> 如果 push 時要你輸入密碼，那個密碼**不是** GitHub 登入密碼，是 Personal Access Token。
> 到 [github.com/settings/tokens](https://github.com/settings/tokens) → Generate new token (classic) →
> 勾 `repo` → 產生出來的那串就是密碼。

推完之後重新整理 GitHub 頁面，應該看得到檔案。

---

## 第 2 步：先開資料庫（Neon）

**先開資料庫再開網站**，因為等一下設定網站時要用到資料庫的連線字串。

1. 到 [neon.com](https://neon.com) → **Sign up** → 用 GitHub 帳號登入
2. 它會叫你建第一個 Project：
   - Project name 填 `mealvote`
   - Region 選 **Singapore** 或 **Tokyo**（離台灣近，快一點）
   - 按 **Create**
3. 建好之後畫面上會有一塊 **Connection string**，長得像：

   ```
   postgresql://neondb_owner:xxxxx@ep-xxxx-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require
   ```

4. **複製它，先貼到記事本**，等一下要用

> 網址中間有 `-pooler` 的那個版本是「連線池」版本，**要用這個**。
> serverless 每次請求都可能是新的連線，沒有連線池會很快把連線數用完。
> Neon 預設給你的就是 pooler 版本，通常不用特別切換。

---

## 第 3 步：開網站（Vercel）

1. 到 [vercel.com](https://vercel.com) → **Sign Up** → 用 **GitHub 帳號**登入
2. Dashboard 按 **Add New...** → **Project**
3. 在 repo 清單裡找到 `mealvote` → 按 **Import**
   - 找不到的話按 **Adjust GitHub App Permissions**，把 repo 的存取權給 Vercel
4. 設定畫面：
   - **Framework Preset**：它應該會顯示 `Other`，維持這樣就好
   - Build / Output / Install 全部**不用改**
5. 展開 **Environment Variables**，加一個：

   | Name | Value |
   |---|---|
   | `DATABASE_URL` | 貼上第 2 步複製的那一長串 |

   > 這一步很重要。沒設的話網站會用檔案模式跑，而 serverless 的檔案系統每次請求都是新的，
   > 你會遇到「建好的房間下一秒就找不到」。

6. 按 **Deploy**

---

## 第 4 步：等它跑完

第一次大約 1–2 分鐘。跑完會看到一個煙火動畫跟你的網址，
長得像 `https://mealvote-xxxx.vercel.app`。

---

## 第 5 步：確認資料庫真的接上了

**這一步不要跳過。**

瀏覽器打開 `https://你的網址/healthz`，應該看到：

```json
{"ok":true,"storage":"pg","rooms":0}
```

- `"storage":"pg"` ✅ 資料庫接上了
- `"storage":"file"` ❌ 環境變數沒吃到。回 Vercel → 專案 → **Settings** → **Environment Variables**
  確認 `DATABASE_URL` 有存好，然後到 **Deployments** → 最新那一筆右邊的 `...` → **Redeploy**

  > 環境變數改了**一定要重新部署才會生效**，這是最常見的卡點。

然後開網址本身，應該看到「🍽️ 朋友約飯神器」的首頁，
最下面會寫「收錄雙北 323 家餐廳 · 台北捷運 139 站」。

---

## 第 6 步：自己先跑一次完整流程

在丟給朋友之前，自己確認一遍：

1. 建一個飯局 → 填自己的條件
2. 開一個**無痕視窗**，貼上房間連結，用另一個名字填第二個人
3. 按「產生餐廳推薦」
4. 兩個視窗應該各自跳出**不同的角色卡**，以及**同一間**神秘店家
5. 神秘店家的彈窗有 10 秒倒數，選「賭一把」或「算了」
6. 兩邊都送出投票 → 自動揭曉，看得到票數、技能結算、角色揭曉、神秘店家真面目

都正常就可以分享了。

---

## 第 7 步：分享

網址直接丟群組。房間連結本身就是入場券，不需要登入、不用註冊、不收 Email。

---

## Vercel 跟 Render 差在哪（為什麼改用這個）

| | Vercel + Neon | Render 免費方案 |
|---|---|---|
| 閒置會不會睡著 | 不會（每次請求即時啟動） | 會，15 分鐘就休眠，醒來要等約 1 分鐘 |
| 要不要自己掛 keep-alive | 不用 | 要，不然朋友點連結會以為壞了 |
| 資料庫會不會到期 | Neon 免費方案永久 | 免費 PostgreSQL 建立後 30 天到期 |
| 信用卡 | 不用 | 不用 |

代價是 Vercel 的免費方案僅限**非商業用途**，而且程式必須寫成「不依賴記憶體狀態」——
這份程式已經改好了：每一次請求都直接讀寫資料庫，改資料時還會把那個房間鎖起來，
所以兩個人同時按送出也不會互相蓋掉。

---

## 之後要改東西怎麼辦

改完程式碼之後：

```bash
git add -A
git commit -m "改了什麼"
git push
```

Vercel 偵測到 push 會自動重新部署，不用回去按任何東西。

---

## 本機要怎麼跑

**不接資料庫**（最簡單，資料存成 `storage/rooms.json`）：

```bash
npm install
npm start
# → http://localhost:3000
```

**接資料庫**（想在本機測跟線上一樣的行為）：

```bash
DATABASE_URL="貼上 Neon 的連線字串" npm start
```

> 注意這樣會直接對**線上的資料庫**讀寫，測試用的房間會跑到正式資料裡。
> 想乾淨一點的話在 Neon 開第二個 branch 當測試環境。

---

## 出問題的時候看哪裡

| 症狀 | 看哪裡 |
|---|---|
| 部署失敗 | Vercel 專案 → **Deployments** → 點那一筆 → **Building** 的 log |
| 網站打得開但 API 都壞掉 | 專案 → **Logs** 分頁（Hobby 方案只留 1 小時，要當下看） |
| `/healthz` 顯示 `"storage":"file"` | 環境變數沒設或設完沒重新部署 |
| 房間建好卻找不到 | 同上。這正是沒接資料庫的典型症狀 |
| 第一次點稍微慢一下 | 正常。Neon 閒置 5 分鐘會休眠，喚醒約半秒 |
| 一直轉圈圈 | Hobby 方案的函式最長 10 秒（設定檔寫 20 秒，實際以方案為準）。正常操作遠低於這個 |

---

## 想改回 Render 也可以

程式碼兩邊都能跑。Render 的話：

1. `render.yaml` 已經在專案裡了，Dashboard → **New +** → **Blueprint** → 選 repo → Apply
2. 它會一起建好網站跟免費 PostgreSQL，連線字串自動注入
3. 記得掛一個 keep-alive（[cron-job.org](https://cron-job.org) 每 10 分鐘打 `/healthz`），不然會休眠
4. 免費 PostgreSQL 30 天到期，到時候建一個新的、換掉 `DATABASE_URL`、重新部署就好（不用搬資料）
